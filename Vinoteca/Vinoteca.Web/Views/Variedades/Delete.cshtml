@model Vinoteca.Web.ViewModels.Variedad.VariedadListVm

@{
    ViewBag.Title = "Delete";
}

<h2>Borrar Variedad</h2>

<h3>¿Está seguro/a que desea eliminar la siguiente variedad?</h3>
<div>
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.NombreVariedad)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.NombreVariedad)
        </dd>

    </dl>

    @using (Html.BeginForm()) {
        @Html.AntiForgeryToken()

        <div class="form-actions no-color">
            <input type="submit" value="Borrar" class="btn btn-primary" /> |
            @Html.ActionLink("Volver", "Index", null, new {@class="btn btn-success"})
        </div>
    }
</div>
